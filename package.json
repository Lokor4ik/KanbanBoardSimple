{
  "name": "root_kanban",
  "version": "1.0.0",
  "description": "This is root directory of the kanban project",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "npm --prefix api/ run start",
    "app": "npm --prefix app/ run start"
  },
  "author": "Pavel Volovich"
}
